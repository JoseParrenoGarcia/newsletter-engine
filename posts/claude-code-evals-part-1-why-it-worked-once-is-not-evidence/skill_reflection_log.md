## 2026-07-07 — /review

**Three iterations exhausted without reaching Ready.** The review loop ran 3 times per the pipeline spec. Actionability (Pass 5) scored 3/5 in all three iterations despite two targeted fixes to the engagement section. The remaining gap — the eval-seed guidance tells readers *what* to produce but not *how* to derive the criteria — is a known, scoped fix that was not applied because the 3-iteration cap was reached. The post ended at average 4.2/5 with all other dimensions ≥ 4. Per pipeline rules, the session surfaced this to Jose rather than proceeding to promote automatically.

**ToC required but flagged as anti-pattern.** The voice critic flagged the `## What will we cover in this post?` section as violating the "excessive preview text" anti-pattern in `anti_patterns.md` (five bullets with explainers exceeds one-sentence-of-framing maximum). However, this section is a required structural element per the `/draft` skill instructions and the series-genai style guide. There is a genuine conflict between the anti-patterns rule and the structural template. The ToC was trimmed to lean one-liners as a compromise, which satisfied the voice critic to 4/5 without removing the required element. The anti-patterns file should note that the ToC structural element is exempt from the excessive-preview-text rule, or the style guide should specify a maximum bullet count.

**`## Closing thoughts` vs `## What comes next` naming.** The iteration 1 draft used `## What comes next` as the closing section name. The structure critic correctly flagged this as a forward-pointer (not synthesis), triggering a ✗ on the closing element. Rename to `## Closing thoughts` and add synthesis content was the fix. The `/draft` skill instructions specify the closing section must be "named ##" but do not prescribe the heading text — any name would pass the detector. Clearer guidance (e.g. "must synthesise the post argument, not just point forward") would catch this earlier.

**Reader questions near-duplication.** Iteration 1 produced questions 1 and 3 that were near-identical (both asked whether the reader had measured their trusted workflow). The impact critic identified this as a structural weakness in iteration 2. The `/draft` skill instructs "3 distinct questions tied to different facets of the argument" — the duplication occurred because both questions were derived from the same facet (trust vs. measurement). A guardrail that explicitly requires each question to reference a *different section* of the post would prevent this.

**Structural fix applied outside the revise skill.** The three priority actions after iterations 1 and 2 were applied directly to `long_draft.md` in the main session (not via a subagent or a revise re-run). The `/new-post` pipeline spec says to "apply targeted editorial fixes directly to `long_draft.md`" after a non-Ready verdict, so this was correct — but the edits bypassed the revise backup step (no new `long_draft_pre-revise.md` was written). This means the backup reflects the SEO-revised draft, not the state before iteration-2 editorial fixes. Not a blocking issue, but worth noting.

## 2026-07-07 — /research

**Connection drop mid-response:** The research subagent was spawned via `Agent` tool and the connection closed mid-response before it returned. Used `SendMessage` to resume the same agent by ID. The agent had already begun (6 tool calls completed) but had not yet written any files. Resume worked cleanly — no data loss, no duplicate work.

**No other issues.** All 13 URLs in `notes.md` validated successfully, 2 gap-filling searches succeeded on first attempt, all 8 ToC sections covered without a second pass. `post.yaml` and `research_brief.md` written correctly on first write.
